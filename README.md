# CopperHead

Pushes desired traffic through a nominated Network Adapter via hostname and routing table.

---

A separate overlay alignment tool lives in [`align-overlay/`](align-overlay/) (**AlignOverlay**). It is not part of CopperHead.
